# 주간 일지 자동 취합 및 아카이브(백업) 시스템 구현 계획서

사용자님의 제안처럼, 별도의 서버나 데이터베이스(Firebase 등)를 구축하지 않고도 브라우저 환경에서 데이터 유실을 방지하고 쾌적한 속도를 유지할 수 있는 아주 현실적이고 효율적인 접근 방식입니다.

앱 내에 **'주간 마감 및 백업 자동화'** 기능과 **'과거 아카이브 열람'** 기능을 추가하여 제안하신 아이디어를 구현하는 계획입니다.

## ⚠️ User Review Required (사용자 확인 필요)

> [!IMPORTANT]
> 순수 프론트엔드(브라우저) 환경에서는 파일이 사용자 PC나 태블릿의 '다운로드' 폴더에 물리적 파일 형태로 저장되어야 합니다. 자동 저장 시 브라우저 보안 정책상 사용자에게 다운로드 프롬프트(알림)가 뜰 수 있습니다.

## ❓ Open Questions

> [!WARNING]
> 1. **다운로드 파일 형식**: 취합된 데이터를 저장할 때, 앱에서 다시 불러와서(Import) 보기 쉬운 **JSON 데이터 파일**로 저장하는 것이 좋을까요, 아니면 엑셀에서 바로 열어볼 수 있는 **CSV/Excel 파일** 형식도 같이 압축해서 제공하는 것이 좋을까요?
> 2. **오래된 데이터 삭제 여부**: 백업 파일을 다운로드(마감 처리)한 이후에는, 브라우저 속도 저하를 막기 위해 기기(localStorage)에 저장된 지난주 데이터는 자동으로 지우는 것이 좋겠죠?

## 🛠️ Proposed Changes

### 1. 주간 데이터 마감 및 압축 파일 자동 생성 기능

- **자동 감지 로직**: 앱이 실행될 때 현재 날짜를 확인하여, 이전 주(월요일~일요일)의 일지 데이터가 남아있고 아직 마감되지 않았다면 관리자에게 **"지난주 일지 주간 마감 및 백업"** 팝업을 띄웁니다.
- **데이터 취합 및 압축**:
  - 지난 주에 생성된 모든 공정별 일지, 생산 수량, 불량 유형, 특이사항(비가동)을 하나의 데이터 셋으로 취합합니다.
  - 이를 브라우저 메모리 상에서 압축(ZIP 또는 JSON 텍스트 포맷)하여 `Stellantis_Weekly_Log_2026_8월_2주차.json` 과 같은 파일명으로 **자동 다운로드**시킵니다.
- **로컬 용량 확보**: 안전하게 파일이 저장(다운로드)된 것을 확인한 후, 브라우저(`localStorage`)에서는 해당 과거 데이터를 지워 앱을 항상 가볍고 빠르게 유지합니다.

### 2. 과거 백업 파일 열람(Viewer) 모드 추가

- **아카이브 열람 UI**: 관리자 대시보드에 **[과거 일지 아카이브 불러오기]** 버튼과 드래그 앤 드롭 영역을 추가합니다.
- **격리된 뷰어 환경**: 다운로드 해두었던 주간 백업 파일(`.json`)을 드래그해서 넣으면, 현재 진행 중인 이번 주 데이터와 섞이지 않도록 **'읽기 전용 아카이브 모드'**로 대시보드가 전환됩니다.
- 이 모드에서는 과거의 총 생산량, 불량률, 특이사항 내역 등을 차트와 표 형태로 다시 조회하고 분석할 수 있습니다.

### 3. 파일 변경 내역 (`index.html`)

#### [MODIFY] `index.html` (Javascript 로직)
- `ArchiveManager` 클래스를 신설하여 주간 단위 날짜 계산, 백업 파일 생성(Blob API 활용), 파일 Import 및 파싱 로직을 전담시킵니다.
- 기존 `DataStore`에 백업 완료 시 해당 기간 데이터를 일괄 삭제하는 로직 추가.

#### [MODIFY] `index.html` (HTML UI)
- 관리자 화면(`#view-manager`) 상단에 아카이브 파일 업로드 컴포넌트 추가.
- 주간 마감 자동 알림 모달 창(Modal) 추가.

## 🧪 Verification Plan

### 수동 검증 (Manual Verification)
1. 시스템 날짜를 인위적으로 다음 주로 변경한 뒤, 앱 접속 시 "주간 마감" 알림이 정상적으로 뜨고 파일이 다운로드되는지 확인합니다.
2. 다운로드된 파일을 다시 앱의 아카이브 뷰어에 업로드했을 때, 과거의 대시보드 화면(차트 및 통계)이 정확하게 복원되어 표시되는지 확인합니다.
