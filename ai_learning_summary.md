# 자동차 부품 생산 관리 시스템 (Auto Parts Management App) - AI 학습용 프로젝트 요약서

이 문서는 다른 AI 세션이나 새로운 환경에서 본 프로젝트의 전체적인 맥락과 코드 구조, 그리고 지금까지 진행된 작업 내역을 단번에 이해시키기(학습시키기) 위해 작성된 요약서입니다. 다음 작업을 지시할 때 이 내용을 복사하여 AI에게 제공하면 완벽하게 문맥을 파악할 수 있습니다.

---

## 1. 프로젝트 개요 및 기술 스택
- **프로젝트 목적:** 자동차 부품 생산 라인의 근로자가 작업 일지(TPM 점검, 생산량, 불량 내용, 비가동 등)를 모바일/PC로 간편하게 입력하고, 관리자가 이를 대시보드 형태로 실시간 집계 및 모니터링하기 위한 웹 애플리케이션.
- **기술 스택:** 
  - **Front-end:** HTML5, Vanilla JavaScript (ES6+), CSS3 (외부 라이브러리로 Remix Icon, Chart.js, HTML5-QRCode 사용)
  - **Back-end & DB:** 별도의 백엔드 없이 모든 데이터는 브라우저의 `localStorage` 기반 (안전한 스토리지 래퍼 `_safeStorage` 사용)으로 저장됨. (로컬 PWA 스타일)
  - **배포:** FTP를 통해 PHP 서버(112.175.185.132)의 HTML 디렉토리에 `index.html` 단일 파일(Single File Application) 형태로 배포됨.

---

## 2. 코드 아키텍처 (index.html 단일 구조)
앱 전체가 `index.html` 파일 하나에 모듈화되어 작성되어 있습니다.
1. **CSS 영역 (`<style>`):** 반응형 웹(모바일 우선), CSS 변수(Variables) 기반의 컬러 팔레트(다크 톤 위주), 커스텀 모달(Modal) 및 배지(Badge) 스타일링.
2. **HTML DOM 영역 (`<body>`):** 
   - `#view-worker`: 근로자 모드 뷰 (작업 전 점검, 생산량 입력, 불량 내역 입력, 작업 이력 등)
   - `#view-manager`: 관리자 모드 뷰 (KPI 대시보드, 차트, 비가동 현황, 생산 로그 리스트 등)
   - 각종 팝업/모달 (비상 알림 모달, 관리자 로그인 모달, QR 스캐너 등)
3. **JavaScript 로직 영역 (`<script>`):**
   - **`DataStore` 클래스:** `localStorage` 데이터를 읽고 쓰는 전역 상태 관리자 (옵저버 패턴 사용).
   - **`WorkerManager` 클래스:** 근로자 모드의 UI 상태 전환(Steps 1~3), 입력 폼 검증, TPM 로직, 내 작업 이력 렌더링.
   - **`ManagerDashboard` 클래스:** 관리자 모드의 차트 렌더링(Chart.js), 프로세스별 집계(총생산량, 양품, 불량률, 달성률 등) 및 정렬, CSV 다운로드 기능.
   - **유틸리티 및 초기화:** 글로벌 `app` 객체에 클래스들을 인스턴스화하고, `INITIAL_HIERARCHY`, `DEFECT_TYPES` 등의 상수(데이터 스키마)를 관리.

---

## 3. 핵심 비즈니스 로직 및 스키마
- **데이터 계층 (Hierarchy):** `제조사(Stellantis 등) -> 차종(DT CREW/QUAD, DS CREW/STD 등) -> 부품(D/SIDE, HOOD SURROUND 등) -> 공정(클립머신, 소재준비, 조인트, 후가공, 검사포장)` 순으로 하향식(Tree) 선택 구조.
- **TPM (작업 전 점검) 로직:** 당일 동일 공정에 대해 최초 1회만 '출근 시간'을 기록하고, 동일한 날 추가 작성 시에는 TPM 점검만 수행(출근 시간은 최초 시간 유지).
- **비가동(Downtime) 로직:** 비가동 사유 발생 시 `downtimeEntries`에 기록되며, 관리자 화면에 즉시 빨간색 배지와 팝업(비가동 경고)으로 실시간 표시됨. 동일한 비가동 알림은 세션당 1회만 뜨도록 `dismissedAlerts` 쿠키로 제어.

---

## 4. 지금까지 진행된 주요 수정 및 추가 작업 연혁 (Changelog)

**[초기 개설]**
- 단일 `index.html` 구조로 HTML/CSS/JS 및 Chart.js 연동 템플릿 뼈대 구축 완료.

**[데이터 및 UI 최적화]**
- **계층 구조 반영:** 사용자 요청에 따라 "공정명 뒤에 '--공정' 붙이지 말 것" 규칙 적용 완료 및 DT/DS 차종별 공정(소재준비 A, C, D 등) 하드코딩 반영.
- **모바일 UI 픽스:** 스마트폰 UI 상단부 블록 크기를 줄이고 줄간격을 조정하여 글자 줄바꿈(레이아웃 깨짐) 현상 수정.

**[기능 추가 및 버그 픽스]**
- **TPM 출근 기록 개선:** 하루에 여러 번 일지 작성 시, 최초에만 출근 시간을 기록하고 이후엔 기록하지 않도록 `WorkerManager._saveLog` 로직 수정.
- **보안 및 알림창 수정:** 
  - 관리자 로그인 시 브라우저 기본 `prompt` 창을 없애고, 비밀번호가 (*) 기호로 가려지는 커스텀 보안 모달(Password Modal) 창으로 교체. 초기 비밀번호(12345604) 노출 텍스트 삭제.
  - 비가동 신고 모아보기 팝업(비상 알림창)에서 '즉시 열기' 후 '조치완료' 또는 'X'를 눌렀을 때 창이 안 꺼지던 버그 해결.
- **더미 데이터 클렌징:** 프로그램 구동 시 임의로 생성되던 가짜 기록(Seed Data) 및 쿠키를 삭제하여 실전 운영용 데이터베이스(Empty State)로 초기화 처리.

**[최신 추가 기능]**
- **관리자 대시보드 정렬(Sort):** 표 헤더(총생산량, 양품수량, 불량수량, 달성률, 불량률) 클릭 시 **오름차순 ➔ 내림차순 ➔ 기본 정렬(차종/공정순)** 순서로 정렬되는 로직 `ManagerDashboard`에 추가.
- **근로자 일지 삭제 기능:** 근로자 모드 > '나의 작업 이력(tbl-my-history)' 표 우측 상태 배지 옆에 **휴지통(삭제) 아이콘** 추가. 클릭 시 `confirm` 창을 거쳐 `store.deleteLog(id)`를 호출하도록 구현 및 레이아웃(Flex) 통합.

---
*(이 텍스트 전체를 복사하여 새로운 AI 세션의 첫 질문이나 시스템 프롬프트에 붙여넣으시면 됩니다!)*
