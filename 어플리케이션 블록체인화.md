# 블록체인형 실시간 데이터베이스 연동 구현 계획서

현재 이 애플리케이션은 사용자의 브라우저 내장 저장소(`localStorage`)를 사용하여 데이터를 저장하고 있습니다. 이를 **'블록체인 같은 형식'의 실시간 데이터베이스**로 전환하기 위한 아키텍처 및 구현 계획입니다.

## ⚠️ User Review Required (사용자 확인 필요)

> [!IMPORTANT]
> 이 애플리케이션은 서버가 없는 순수 HTML 프론트엔드 환경이므로, 복잡한 자체 백엔드 구축 없이 빠르고 안정적인 실시간 동기화를 지원하는 **BaaS(Backend as a Service)인 Google Firebase(Firestore) 또는 Supabase**를 사용하는 것이 가장 적합합니다. 
> 
> 해당 서비스를 연동하려면 **프로젝트 환경 설정 및 API 키(설정값)**가 필요합니다. 본 계획을 승인해주시면, 무료로 사용할 수 있는 Firebase 프로젝트 생성 방법과 설정값을 넣을 공간을 안내해 드리겠습니다.

## ❓ Open Questions

> [!WARNING]
> 1. **Firebase 사용 여부**: 실시간 데이터베이스로 가장 많이 쓰이는 Firebase를 사용하여 연동을 진행해도 괜찮으신가요?
> 2. **오프라인 동작 여부**: 공장 내부 네트워크 문제로 인터넷이 끊길 경우, 로컬에 임시 저장했다가 인터넷이 연결될 때 다시 동기화하는 기능(Offline Persistence)도 함께 활성화할까요?

## 🛠️ Proposed Changes

### 1. 데이터 저장소 마이그레이션 (`DataStore` 클래스 전면 개편)

- **현행**: `_safeStorage.getItem()` / `setItem()`을 통한 로컬 저장.
- **변경**: Firebase Firestore API를 호출하여 클라우드 데이터베이스에 실시간으로 Read/Write 수행.
- **실시간 리스너 적용**: `onSnapshot` 리스너를 달아, 다른 태블릿이나 PC에서 데이터가 변경되면 현재 화면의 관리자 대시보드 및 일지 이력이 새로고침 없이 즉시(실시간) 업데이트되도록 구성합니다.

### 2. 블록체인형(Append-Only Ledger) 데이터 구조 설계

근로자가 일지를 **생성 및 수정할 때마다 과거 기록이 덮어씌워져 사라지지 않도록** 블록체인의 원장(Ledger) 방식을 차용합니다.

- **`logs` 컬렉션**: 항상 '최신 상태'의 일지 데이터를 보관하여 대시보드 조회 속도를 높입니다.
- **`log_history` 컬렉션 (원장)**: 일지가 생성(CREATE)되거나 수정(UPDATE)될 때마다, 기존 데이터를 덮어쓰지 않고 **새로운 트랜잭션 기록으로 계속 누적(Append)**합니다.
  - **저장 항목**: 수정 시간, 수정한 작업자, 이전 데이터의 해시값(무결성 증명용), 변경된 필드 및 내용.
  - 이를 통해 "누가, 언제, 어떤 수량을 어떻게 수정했는지" 모든 위변조 이력이 영구적으로 투명하게 남게 됩니다.

### 3. 파일 변경 내역 (`index.html`)

#### [MODIFY] `index.html` (스크립트 로드 및 Firebase 초기화)
- `<head>` 태그 내에 Firebase SDK (App, Firestore) CDN 링크 추가.
- 하단에 Firebase Configuration (API 키 등) 설정 코드 추가.

#### [MODIFY] `index.html` (DataStore 클래스)
- `getLogs`, `saveLog`, `updateLog`, `deleteLog` 메서드를 모두 비동기(Promise) 방식으로 변경하고 Firestore와 통신하도록 재작성.
- `updateLog` 호출 시 `logs` 문서를 업데이트함과 동시에 `log_history` 컬렉션에 새 트랜잭션을 `add` 하는 로직 추가.

## 🧪 Verification Plan

### 수동 검증 (Manual Verification)
- **실시간 동기화 테스트**: 브라우저 탭을 2개 띄워놓고(하나는 근로자 모드, 하나는 관리자 모드), 근로자 탭에서 일지를 제출했을 때 관리자 탭이 새로고침 없이 즉시 업데이트되는지 확인합니다.
- **블록체인형 이력 추적 테스트**: 제출된 일지를 다시 불러와 생산 수량을 수정한 뒤 제출합니다. 데이터베이스의 `log_history`를 조회하여 원본 기록과 수정 기록이 모두 훼손 없이 보존되어 있는지 확인합니다.
